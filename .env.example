# Which AI provider to use: mock | deepseek | claude | openai
AI_PROVIDER=mock

# Only needed for the matching provider. Put real values in .env.local (gitignored).
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
